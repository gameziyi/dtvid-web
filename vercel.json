{
  "buildCommand": "npm install --no-save && npx svelte-kit sync && npm run build",
  "devCommand": "npm run dev",
  "installCommand": "true",
  "framework": "sveltekit",
  "outputDirectory": ".svelte-kit/output"
}
